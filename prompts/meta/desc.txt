Task: You should design an efficient metaheuristic using the following constraints in a function format. Your solution code should balance exploration and exploitation creatively. 
**First, describe your metaheuristic, novel or traditional optimization strategy and main optimization steps in one sentence. The description must be inside a brace and marked as a comment.** Next, implement it in Python as a function named "improve_algorithm". This function should accept 5 inputs: 'population', 'utility', 'language_model','function_format','task'. The function should return 3 outputs: 'best_idea','best_solution','best_utility'. 'utility' is a function that evaluates solutions based on a score function, 'function_format' is the format for model responses, 'task' is the name of the problem to be optimized. The function returns 'best_idea','best_solution','best_utility' which are the idea behind best solution, best code together with its utility.
'language_model' is an instance of the language model class used for code generation, with function "def prompt_batch(expertise,message_batch,temperature), return responses_list" for multiple request to LLM and "def prompt(self, expertise, message, temperature) return result" for single request to LLM, 'population' is a dictionary of several historical best solutions of the task, you only use the following functions to operate: "def get_solution_by_index(self, task_name, index): return item", to get a solution by its utility rank; "def get_random_solution(self, task_name): return item", to get a random solution from the population; the item returned above is a dictionary with keys 'best_sol' and 'utility'. Other functions you can use are: "def get_subtask_size(self, task_name):" to return the size of the population. 
Please make sure to include all necessary package imports. Specifically, always add the line "from utils.utils import extract_code,extract_idea\n import json\n" at the beginning of the code to ensure it runs correctly.
**You should use your proposed novel or traditional optimization strategy to select solution in the population, generate different or modified directions, then generate better solutions** You are free to create metaheuristic or totally novel optimization frameworks under this function signature to optimize solutions. Your should develop self-improvement or traditional optimization metaheuristics that can refine the solution by adapting, exploring different possibilities, and enhancing performance. You are encouraged to write different prompts utilizing language model to create innovative prompts for proposing optimization strategies and directions for generating diverse enough directions and better code. Do not restrict yourself to the given parameters; you can create new ones or alternatives. You must use the language model to generate code snippets or directions for optimization instead of modifying solutions locally. Avoid modifying snippets of the solution code, as it may invalidate them. Avoid brute-force or exhaustive search methods. Never generate infinite loops of code that runs indefinitely. Your solution must not rely on repetitive iterations at any given level of the algorithm. This applies to the outer loop controlling the overall process (e.g., while loop).